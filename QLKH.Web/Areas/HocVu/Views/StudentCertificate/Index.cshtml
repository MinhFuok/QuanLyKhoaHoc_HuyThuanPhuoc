@model IEnumerable<QLKH.Domain.Entities.StudentCertificate>

@{
    ViewData["Title"] = "Chứng chỉ học viên";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";

    var keyword = ViewBag.Keyword as string ?? "";
}

@section Styles {
    <link rel="stylesheet" href="~/css/HocVuProfession.css" asp-append-version="true" />

    <style>
        .student-certificate-page .cp-table-wrap {
            width: 100%;
            overflow-x: auto;
        }

        .student-certificate-table {
            width: 100%;
            table-layout: fixed;
        }

            .student-certificate-table th,
            .student-certificate-table td {
                vertical-align: middle;
            }

            .student-certificate-table th {
                white-space: nowrap;
            }

        .student-cell,
        .certificate-title-cell,
        .issuer-cell {
            min-width: 0;
            word-break: break-word;
        }

        .certificate-title-cell,
        .issuer-cell {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            line-height: 1.4;
        }

        .date-cell {
            white-space: nowrap;
            font-weight: 800;
            color: rgba(240, 232, 255, 0.88);
        }

        .certificate-status-cell {
            display: flex;
            justify-content: center;
        }

        .certificate-file-cell {
            display: flex;
            justify-content: center;
        }

        .certificate-actions {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 8px;
            justify-content: stretch;
        }

            .certificate-actions .cp-btn {
                width: 100%;
                min-width: 0;
                justify-content: center;
                padding-left: 10px;
                padding-right: 10px;
            }

        .student-certificate-page .cp-file-link {
            white-space: nowrap;
        }

        .student-certificate-page .cp-muted {
            white-space: nowrap;
        }

        @@media (max-width: 1199.98px) {
            .student-certificate-table {
                min-width: 1150px;
            }
        }

        .certificate-filter-card {
            background: var(--cp-surface, rgba(13, 8, 30, .75));
            border: 1px solid var(--cp-border, rgba(140, 80, 255, .18));
            border-radius: var(--cp-radius, 10px);
            padding: 16px;
            margin: 18px 0;
            box-shadow: var(--cp-shadow, 0 2px 12px rgba(0,0,0,.5));
        }

        .certificate-filter-form {
            display: grid;
            grid-template-columns: minmax(0, 1fr) auto;
            gap: 12px;
            align-items: end;
        }

        .certificate-filter-field label {
            display: block;
            margin-bottom: 6px;
            color: var(--cp-text-sub, rgba(210,185,255,.8));
            font-size: 0.9rem;
            font-weight: 800;
        }

        .certificate-filter-input {
            width: 100%;
            min-height: 42px;
            border: 1px solid var(--cp-border, rgba(140,80,255,.18));
            border-radius: var(--cp-radius-sm, 7px);
            padding: 9px 12px;
            color: var(--cp-text, #f0e8ff);
            background: var(--cp-surface-2, rgba(25,12,55,.65));
            font-weight: 700;
            outline: none;
        }

            .certificate-filter-input::placeholder {
                color: var(--cp-text-muted, rgba(180,150,255,.55));
            }

            .certificate-filter-input:focus {
                border-color: var(--cp-accent, #a855f7);
                box-shadow: 0 0 0 0.18rem var(--cp-accent-light, rgba(168,85,247,.10));
            }

        .certificate-filter-actions {
            display: flex;
            gap: 8px;
        }

            .certificate-filter-actions .cp-btn {
                min-height: 42px;
                white-space: nowrap;
            }

        .certificate-filter-note {
            margin-top: 10px;
            color: var(--cp-text-muted, rgba(180,150,255,.55));
            font-size: 0.9rem;
            font-weight: 700;
        }

        @@media (max-width: 575.98px) {
            .certificate-filter-form {
                grid-template-columns: 1fr;
            }

            .certificate-filter-actions {
                flex-direction: column;
            }

                .certificate-filter-actions .cp-btn {
                    width: 100%;
                }
        }
    </style>
}

<div class="course-page admin-profession-page student-certificate-page">
    <div class="cp-header">
        <div class="cp-header-left">
            <h1 class="cp-title">CẤP CHỨNG CHỈ HỌC VIÊN</h1>
            <p class="cp-subtitle">Danh sách cấp chứng chỉ cho học viên đã hoàn thành khóa học...</p>
        </div>

        <a asp-area="HocVu"
           asp-controller="StudentCertificate"
           asp-action="Create"
           class="cp-btn cp-btn--primary">
            + Cấp chứng chỉ
        </a>
    </div>
    <div class="certificate-filter-card">
        <form asp-area="HocVu"
              asp-controller="StudentCertificate"
              asp-action="Index"
              method="get"
              class="certificate-filter-form">

            <div class="certificate-filter-field">
                <label>Tìm kiếm</label>
                <input type="text"
                       name="keyword"
                       value="@keyword"
                       class="certificate-filter-input"/>
            </div>

            <div class="certificate-filter-actions">
                <button type="submit" class="cp-btn cp-btn--primary">
                    Lọc
                </button>

                <a asp-area="HocVu"
                   asp-controller="StudentCertificate"
                   asp-action="Index"
                   class="cp-btn cp-btn--ghost">
                    Bỏ lọc
                </a>
            </div>
        </form>

        @if (!string.IsNullOrWhiteSpace(keyword))
        {
            <div class="certificate-filter-note">
                Đang tìm kiếm: <strong>@keyword</strong>
            </div>
        }
    </div>
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="cp-inline-alert cp-inline-alert--success" data-cp-alert>
            <div class="cp-inline-alert-icon">✓</div>
            <div class="cp-inline-alert-content">
                <strong>Thao tác thành công</strong>
                <span>@TempData["SuccessMessage"]</span>
            </div>
            <button type="button" class="cp-inline-alert-close" data-cp-alert-close aria-label="Đóng thông báo">×</button>
        </div>
    }

    <div class="cp-table-wrap">
        <table class="cp-table student-certificate-table">
            <colgroup>
                <col style="width: 13%;" />
                <col style="width: 20%;" />
                <col style="width: 12%;" />
                <col style="width: 10%;" />
                <col style="width: 12%;" />
                <col style="width: 10%;" />
                <col style="width: 11%;" />
                <col style="width: 12%;" />
            </colgroup>

            <thead>
                <tr>
                    <th class="text-center">Học viên</th>
                    <th class="text-center">Tên chứng chỉ</th>
                    <th class="text-center">Mã/Số chứng chỉ</th>
                    <th class="text-center">Ngày cấp</th>
                    <th class="text-center">Ngày hết hạn</th>
                    <th class="text-center">Trạng thái</th>
                    <th class="text-center">Minh chứng</th>
                    <th class="cp-col-action text-center">Thao tác</th>
                </tr>
            </thead>

            <tbody>
                @if (Model?.Any() == true)
                {
                    foreach (var item in Model)
                    {
                        <tr class="cp-row">
                            <td>
                                <div class="cp-name student-cell">
                                    @(item.Student?.FullName ?? "Chưa có học viên")
                                </div>
                            </td>

                            <td>
                                <div class="cp-certificate-title certificate-title-cell">
                                    @item.CertificateName
                                </div>
                            </td>

                            <td>
                                @if (!string.IsNullOrWhiteSpace(item.CertificateCode))
                                {
                                    <span class="cp-code">@item.CertificateCode</span>
                                }
                                else
                                {
                                    <span class="cp-muted">Chưa có</span>
                                }
                            </td>

                            <td>
                                <span class="date-cell">
                                    @(item.IssuedDate?.ToString("dd/MM/yyyy") ?? "Chưa có")
                                </span>
                            </td>
                            <td>
                                @if (item.ExpiryDate.HasValue)
                                {
                                    if (item.ExpiryDate.Value.Date < DateTime.Today)
                                    {
                                        <div>
                                            <span class="date-cell">@item.ExpiryDate.Value.ToString("dd/MM/yyyy")</span>
                                            <div class="cp-muted">Đã hết hạn</div>
                                        </div>
                                    }
                                    else
                                    {
                                        <div>
                                            <span class="date-cell">@item.ExpiryDate.Value.ToString("dd/MM/yyyy")</span>
                                            <div class="cp-muted">Còn hiệu lực</div>
                                        </div>
                                    }
                                }
                                else
                                {
                                    <span class="cp-muted">Không thời hạn</span>
                                }
                            </td>
                            <td>
                                <div class="certificate-status-cell">
                                    <div class="cp-status-surface cp-status-surface--@(item.IsApproved ? "confirmed" : "pending")">
                                        <span class="cp-badge cp-badge--@(item.IsApproved ? "confirmed" : "pending")">
                                            @(item.IsApproved ? "Đã xác nhận" : "Chờ xác nhận")
                                        </span>
                                    </div>
                                </div>
                            </td>

                            <td>
                                <div class="certificate-file-cell">
                                    @if (!string.IsNullOrWhiteSpace(item.EvidenceFilePath))
                                    {
                                        <a href="@item.EvidenceFilePath"
                                           target="_blank"
                                           rel="noopener noreferrer"
                                           class="cp-file-link">
                                            Xem file
                                        </a>
                                    }
                                    else
                                    {
                                        <span class="cp-muted">Không có</span>
                                    }
                                </div>
                            </td>

                            <td>
                                <div class="cp-actions certificate-actions">
                                    <a asp-area="HocVu"
                                       asp-controller="StudentCertificate"
                                       asp-action="Details"
                                       asp-route-id="@item.Id"
                                       class="cp-btn cp-btn--info cp-btn--sm">
                                        Chi tiết
                                    </a>

                                    <a asp-area="HocVu"
                                       asp-controller="StudentCertificate"
                                       asp-action="Edit"
                                       asp-route-id="@item.Id"
                                       class="cp-btn cp-btn--warning cp-btn--sm">
                                        Sửa
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="8" class="cp-empty">Chưa có chứng chỉ nào trong hệ thống.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script src="~/js/HocVuProfession.js" asp-append-version="true"></script>
}
