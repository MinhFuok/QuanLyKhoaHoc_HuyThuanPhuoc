@model IEnumerable<QLKH.Domain.Entities.StudentCertificate>

@{
    ViewData["Title"] = "Chá»©ng chá»‰ há»c viÃªn";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";

    var keyword = ViewBag.Keyword as string ?? "";
}

@section Styles {
    <link rel="stylesheet" href="~/css/HocVuProfession.css" asp-append-version="true" />

    <style>
        .student-certificate-page .cp-table-wrap {
            width: 100%;
            overflow-x: auto;
        }

        .student-certificate-table {
            width: 100%;
            table-layout: fixed;
        }

            .student-certificate-table th,
            .student-certificate-table td {
                vertical-align: middle;
            }

            .student-certificate-table th {
                white-space: nowrap;
            }

        .student-cell,
        .certificate-title-cell,
        .issuer-cell {
            min-width: 0;
            word-break: break-word;
        }

        .certificate-title-cell,
        .issuer-cell {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            line-height: 1.4;
        }

        .date-cell {
            white-space: nowrap;
            font-weight: 800;
            color: rgba(240, 232, 255, 0.88);
        }

        .certificate-status-cell {
            display: flex;
            justify-content: center;
        }

        .certificate-file-cell {
            display: flex;
            justify-content: center;
        }

        .certificate-actions {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 8px;
            justify-content: stretch;
        }

            .certificate-actions .cp-btn {
                width: 100%;
                min-width: 0;
                justify-content: center;
                padding-left: 10px;
                padding-right: 10px;
            }

        .student-certificate-page .cp-file-link {
            white-space: nowrap;
        }

        .student-certificate-page .cp-muted {
            white-space: nowrap;
        }

        @@media (max-width: 1199.98px) {
            .student-certificate-table {
                min-width: 1150px;
            }
        }

        .certificate-filter-card {
            background: var(--cp-surface, rgba(13, 8, 30, .75));
            border: 1px solid var(--cp-border, rgba(140, 80, 255, .18));
            border-radius: var(--cp-radius, 10px);
            padding: 16px;
            margin: 18px 0;
            box-shadow: var(--cp-shadow, 0 2px 12px rgba(0,0,0,.5));
        }

        .certificate-filter-form {
            display: grid;
            grid-template-columns: minmax(0, 1fr) auto;
            gap: 12px;
            align-items: end;
        }

        .certificate-filter-field label {
            display: block;
            margin-bottom: 6px;
            color: var(--cp-text-sub, rgba(210,185,255,.8));
            font-size: 0.9rem;
            font-weight: 800;
        }

        .certificate-filter-input {
            width: 100%;
            min-height: 42px;
            border: 1px solid var(--cp-border, rgba(140,80,255,.18));
            border-radius: var(--cp-radius-sm, 7px);
            padding: 9px 12px;
            color: var(--cp-text, #f0e8ff);
            background: var(--cp-surface-2, rgba(25,12,55,.65));
            font-weight: 700;
            outline: none;
        }

            .certificate-filter-input::placeholder {
                color: var(--cp-text-muted, rgba(180,150,255,.55));
            }

            .certificate-filter-input:focus {
                border-color: var(--cp-accent, #a855f7);
                box-shadow: 0 0 0 0.18rem var(--cp-accent-light, rgba(168,85,247,.10));
            }

        .certificate-filter-actions {
            display: flex;
            gap: 8px;
        }

            .certificate-filter-actions .cp-btn {
                min-height: 42px;
                white-space: nowrap;
            }

        .certificate-filter-note {
            margin-top: 10px;
            color: var(--cp-text-muted, rgba(180,150,255,.55));
            font-size: 0.9rem;
            font-weight: 700;
        }

        @@media (max-width: 575.98px) {
            .certificate-filter-form {
                grid-template-columns: 1fr;
            }

            .certificate-filter-actions {
                flex-direction: column;
            }

                .certificate-filter-actions .cp-btn {
                    width: 100%;
                }
        }
    </style>
}

<div class="course-page admin-profession-page student-certificate-page">
    <div class="cp-header">
        <div class="cp-header-left">
            <h1 class="cp-title">Cáº¤P CHá»¨NG CHá»ˆ Há»ŒC VIÃŠN</h1>
            <p class="cp-subtitle">Danh sÃ¡ch cáº¥p chá»©ng chá»‰ cho há»c viÃªn Ä‘Ã£ hoÃ n thÃ nh khÃ³a há»c...</p>
        </div>

        <a asp-area="HocVu"
           asp-controller="StudentCertificate"
           asp-action="Create"
           class="cp-btn cp-btn--primary">
            + Cáº¥p chá»©ng chá»‰
        </a>
    </div>
    <div class="certificate-filter-card">
        <form asp-area="HocVu"
              asp-controller="StudentCertificate"
              asp-action="Index"
              method="get"
              class="certificate-filter-form">

            <div class="certificate-filter-field">
                <label>TÃ¬m kiáº¿m</label>
                <input type="text"
                       name="keyword"
                       value="@keyword"
                       class="certificate-filter-input"/>
            </div>

            <div class="certificate-filter-actions">
                <button type="submit" class="cp-btn cp-btn--primary">
                    Lá»c
                </button>

                <a asp-area="HocVu"
                   asp-controller="StudentCertificate"
                   asp-action="Index"
                   class="cp-btn cp-btn--ghost">
                    Bá» lá»c
                </a>
            </div>
        </form>

        @if (!string.IsNullOrWhiteSpace(keyword))
        {
            <div class="certificate-filter-note">
                Äang tÃ¬m kiáº¿m: <strong>@keyword</strong>
            </div>
        }
    </div>
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="cp-inline-alert cp-inline-alert--success" data-cp-alert>
            <div class="cp-inline-alert-icon">âœ“</div>
            <div class="cp-inline-alert-content">
                <strong>Thao tÃ¡c thÃ nh cÃ´ng</strong>
                <span>@TempData["SuccessMessage"]</span>
            </div>
            <button type="button" class="cp-inline-alert-close" data-cp-alert-close aria-label="ÄÃ³ng thÃ´ng bÃ¡o">Ã—</button>
        </div>
    }

    <div class="cp-table-wrap">
        <table class="cp-table student-certificate-table">
            <colgroup>
                <col style="width: 13%;" />
                <col style="width: 20%;" />
                <col style="width: 12%;" />
                <col style="width: 10%;" />
                <col style="width: 12%;" />
                <col style="width: 10%;" />
                <col style="width: 11%;" />
                <col style="width: 12%;" />
            </colgroup>

            <thead>
                <tr>
                    <th class="text-center">Há»c viÃªn</th>
                    <th class="text-center">TÃªn chá»©ng chá»‰</th>
                    <th class="text-center">MÃ£/Sá»‘ chá»©ng chá»‰</th>
                    <th class="text-center">NgÃ y cáº¥p</th>
                    <th class="text-center">NgÃ y háº¿t háº¡n</th>
                    <th class="text-center">Tráº¡ng thÃ¡i</th>
                    <th class="text-center">Minh chá»©ng</th>
                    <th class="cp-col-action text-center">Thao tÃ¡c</th>
                </tr>
            </thead>

            <tbody>
                @if (Model?.Any() == true)
                {
                    foreach (var item in Model)
                    {
                        <tr class="cp-row">
                            <td>
                                <div class="cp-name student-cell">
                                    @(item.Student?.FullName ?? "ChÆ°a cÃ³ há»c viÃªn")
                                </div>
                            </td>

                            <td>
                                <div class="cp-certificate-title certificate-title-cell">
                                    @item.CertificateName
                                </div>
                            </td>

                            <td>
                                @if (!string.IsNullOrWhiteSpace(item.CertificateCode))
                                {
                                    <span class="cp-code">@item.CertificateCode</span>
                                }
                                else
                                {
                                    <span class="cp-muted">ChÆ°a cÃ³</span>
                                }
                            </td>

                            <td>
                                <span class="date-cell">
                                    @(item.IssuedDate?.ToString("dd/MM/yyyy") ?? "ChÆ°a cÃ³")
                                </span>
                            </td>
                            <td>
                                @if (item.ExpiryDate.HasValue)
                                {
                                    if (item.ExpiryDate.Value.Date < DateTime.Today)
                                    {
                                        <div>
                                            <span class="date-cell">@item.ExpiryDate.Value.ToString("dd/MM/yyyy")</span>
                                            <div class="cp-muted">ÄÃ£ háº¿t háº¡n</div>
                                        </div>
                                    }
                                    else
                                    {
                                        <div>
                                            <span class="date-cell">@item.ExpiryDate.Value.ToString("dd/MM/yyyy")</span>
                                            <div class="cp-muted">CÃ²n hiá»‡u lá»±c</div>
                                        </div>
                                    }
                                }
                                else
                                {
                                    <span class="cp-muted">KhÃ´ng thá»i háº¡n</span>
                                }
                            </td>
                            <td>
                                <div class="certificate-status-cell">
                                    <div class="cp-status-surface cp-status-surface--@(item.IsApproved ? "confirmed" : "pending")">
                                        <span class="cp-badge cp-badge--@(item.IsApproved ? "confirmed" : "pending")">
                                            @(item.IsApproved ? "ÄÃ£ xÃ¡c nháº­n" : "Chá» xÃ¡c nháº­n")
                                        </span>
                                    </div>
                                </div>
                            </td>

                            <td>
                                <div class="certificate-file-cell">
                                    @if (!string.IsNullOrWhiteSpace(item.EvidenceFilePath))
                                    {
                                        <a href="@item.EvidenceFilePath"
                                           target="_blank"
                                           rel="noopener noreferrer"
                                           class="cp-file-link">
                                            Xem file
                                        </a>
                                    }
                                    else
                                    {
                                        <span class="cp-muted">KhÃ´ng cÃ³</span>
                                    }
                                </div>
                            </td>

                            <td>
                                <div class="cp-actions certificate-actions">
                                    <a asp-area="HocVu"
                                       asp-controller="StudentCertificate"
                                       asp-action="Details"
                                       asp-route-id="@item.Id"
                                       class="cp-btn cp-btn--info cp-btn--sm">
                                        Chi tiáº¿t
                                    </a>

                                    <a asp-area="HocVu"
                                       asp-controller="StudentCertificate"
                                       asp-action="Edit"
                                       asp-route-id="@item.Id"
                                       class="cp-btn cp-btn--warning cp-btn--sm">
                                        Sá»­a
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="8" class="cp-empty">ChÆ°a cÃ³ chá»©ng chá»‰ nÃ o trong há»‡ thá»‘ng.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script src="~/js/HocVuProfession.js" asp-append-version="true"></script>
}
